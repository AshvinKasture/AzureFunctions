{
  "name": "func-worker-common-prod-cin",
  "version": "1.0.0",
  "description": "Azure Worker Function App for scheduled background jobs",
  "main": "src/functions/*.js",
  "scripts": {
    "start": "func start",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "azure-functions-core-tools": "^4.x"
  }
}
